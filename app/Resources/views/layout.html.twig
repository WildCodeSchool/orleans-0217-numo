{% extends 'base.html.twig' %}


{% block header %}
    <nav id="mainNav" class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->

            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span> Menu <i class="fa fa-bars"></i>
                </button>
                <a class="navbar-brand page-scroll" href="{{ path('index') }}">
                    <img src="{{ asset('img/NUMOsmall.png') }}" class="img-responsive navbar-logo" alt="numo logo">
                </a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav">
                        <li>
                            <a class="page-scroll" href="{{ path('association') }}">Association</a>

                        </li>
                        <li>
                            <a class="page-scroll" href="{{ path('event_list_published') }}">Agenda</a>
                        </li>
                        <li>
                            <a class="page-scroll" href="{{ path('members') }}" >Membres</a>
                        </li>
                        <li>
                            <a class="page-scroll" href="{{ path('become_adherent') }}" >Devenez Adhérent</a>
                        </li>
                        <li>
                            <a class="page-scroll" href="{{ path('contact') }}">Contact</a>
                        </li>
                        <li>
                            <span class="glyphicon glyphicon-user" aria-hidden="true"></span>
                        </li>
                        <li>
                            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                                <span class="glyphicon glyphicon-bell navbar-right" aria-hidden="true"></span>
                                <span class="glyphicon glyphicon-cog navbar-right" aria-hidden="true"></span>
                                <p>{{ app.user.name }} {{ app.user.firstname }}</p>
                                <a href="{{ path('fos_user_security_logout') }}" class="btn btn-default navbar-right">Deconnexion</a>
                                <a href="{{ path('fos_user_profile_show') }}" class="btn btn-default navbar-right">Profil</a>
                            {% else %}
                                <a href="{{ path('fos_user_security_login') }}" class="btn btn-default">Connexion</a>
                        </li>
                        <li>
                                <a href="{{ path('fos_user_registration_register') }}" class="btn btn-default inscription">S'inscrire</a>
                        </li>
                            {%endif%}
                    </ul>
                </div>
        </div>
        <!-- /.container-fluid -->
    </nav>

{% endblock %}


{% block footer %}
    {% include 'footer.html.twig' %}
{% endblock %}
