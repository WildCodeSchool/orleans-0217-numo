{% extends 'layout.html.twig' %}

{% trans_default_domain 'FOSUserBundle' %}

{% block body %}

    <header>
        <div class="header-content">
            <div class="header-content-inner">

                <h2 id="homeHeading">Connexion</h2>
                <hr>

                {% if error %}
                    <div class="text-center">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

                <form action="{{ path("fos_user_security_check") }}" method="post">

                    {% if csrf_token %}
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                    {% endif %}

                    <div class="form-group">
                        <label class="col-sm-3 col-sm-offset-3" for="username">{{ 'security.login.username'|trans }}</label>
                        <input class="col-sm-2" type="text" id="username" name="_username" value="{{ last_username }}" required="required" />
                    </div>

                    <div class="form-group">
                        <label class="col-sm-3 col-sm-offset-3" for="password">{{ 'security.login.password'|trans }}</label>
                        <input class="col-sm-2 input" type="password" id="password" name="_password" required="required">
                    </div>

                    <div class="col-sm-6 col-sm-offset-3 text-center">
                        <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
                        <label for="remember_me">{{ 'security.login.remember_me'|trans }}</label>

                        <input type="submit" class="col-sm-offset-1 btn btn-default btn-xl sr-button" id="_submit" name="_submit"
                               value="{{ 'security.login.submit'|trans }}" />
                    </div>

                </form>

            </div>
        </div>
    </header>



{% endblock %}